#include <iostream>
#include <string>

using namespace std;

struct Node {
  int num;
  Node* next; 
};

void BuildList(Node* &head, Node* &tail) {
  int i;
  Node* move;

  head = tail = move = NULL;

  while ( 
  
  
}
